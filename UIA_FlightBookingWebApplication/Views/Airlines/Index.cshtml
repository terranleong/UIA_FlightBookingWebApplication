@model IEnumerable<UIA_FlightBookingWebApplication.Models.Airline>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.airlName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.airlName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.airlID }) |
            @Html.ActionLink("Details", "Details", new { id=item.airlID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.airlID })
        </td>
    </tr>
}

</table>
