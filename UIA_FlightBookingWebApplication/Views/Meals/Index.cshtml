@model IEnumerable<UIA_FlightBookingWebApplication.Models.Meal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mCategory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mCategory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.mID }) |
            @Html.ActionLink("Details", "Details", new { id=item.mID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.mID })
        </td>
    </tr>
}

</table>
